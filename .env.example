PORT=4000
MONGO_URI=
DB_NAME=oxford-dic
COLLECTION_NAME=words

# Google AI Studio Configuration
GOOGLE_API_KEY=
GOOGLE_MODEL=gemini-2.5-flash

# JWT Configuration
JWT_SECRET=your_jwt_secret_change_this_in_production
JWT_REFRESH_SECRET=your_jwt_refresh_secret_change_this_in_production

# Email Configuration (Gmail SMTP)
EMAIL_USER=duyld2306@gmail.com
EMAIL_PASS=

# Frontend URL for verify/reset links
CLIENT_URL=http://localhost:3000

# Token Expiration
ACCESS_TOKEN_EXPIRE=15d
REFRESH_TOKEN_EXPIRE=7d

# Google OAuth2 credentials - create in Google Cloud Console
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Redirect URI should match one configured in OAuth consent (e.g. http://localhost:3000/oauth2callback)
GOOGLE_REDIRECT_URI=http://localhost:3000/oauth2callback

# Optional: Drive folder id to upload backups into. Leave empty to upload to root
GOOGLE_FOLDER_ID=backup
